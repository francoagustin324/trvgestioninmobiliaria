name: Release A3.4 + WebKit-572 Reconcile (archived)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    runs-on: ubuntu-24.04
    steps:
      - run: echo "Reconciliation already completed; validation runs separately."
