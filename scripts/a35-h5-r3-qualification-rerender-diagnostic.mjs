import { readFileSync, writeFileSync } from 'node:fs';

const source = 'dist/tests/b1-2-3-compact-leads-real-app.test.js';
const target = 'dist/tests/b1-2-3-compact-leads-real-app.r3diag.test.js';
let text = readFileSync(source, 'utf8');

const needle = `    await panel.locator('[data-analyze-qualification]').click();\n    await panel.locator('[data-apply-qualification]').waitFor({ state: 'visible' });\n    const controls = [`;
const replacement = `    let __r3ReleaseConfig;\n    const __r3ConfigGate = new Promise((resolve) => { __r3ReleaseConfig = resolve; });\n    let __r3OptionalConfigIntercepted = false;\n    await page.route('**/api/cloud-config', async (route) => {\n        if (!__r3OptionalConfigIntercepted) {\n            __r3OptionalConfigIntercepted = true;\n            await __r3ConfigGate;\n        }\n        await route.fallback();\n    });\n    await panel.locator('[data-analyze-qualification]').click();\n    await panel.locator('[data-apply-qualification]').waitFor({ state: 'visible' });\n    const __r3OldLocator = panel.locator('[data-suggestion-value]:not([disabled]), [data-qualification-text]').last();\n    const __r3OldNode = await __r3OldLocator.elementHandle();\n    if (!__r3OldNode) throw new Error('R3_DIAG_NO_DETERMINISTIC_NODE');\n    await __r3OldLocator.focus();\n    const __r3Before = await __r3OldNode.evaluate((element) => ({\n        connected: element.isConnected,\n        active: document.activeElement === element,\n        tag: element.tagName,\n        suggestion: element.closest('[data-qualification-suggestion]')?.getAttribute('data-qualification-suggestion') || null,\n    }));\n    console.log('R3_QUALIFICATION_NODE_BEFORE=' + JSON.stringify(__r3Before));\n    __r3ReleaseConfig();\n    await panel.locator('.qualification-info').waitFor({ state: 'visible' });\n    await page.waitForFunction((node) => !node.isConnected, __r3OldNode);\n    const __r3AfterOld = await __r3OldNode.evaluate((element) => ({\n        connected: element.isConnected,\n        active: document.activeElement === element,\n        tag: element.tagName,\n        suggestion: element.closest('[data-qualification-suggestion]')?.getAttribute('data-qualification-suggestion') || null,\n    }));\n    const __r3CurrentLocator = panel.locator('[data-suggestion-value]:not([disabled]), [data-qualification-text]').last();\n    const __r3CurrentNode = await __r3CurrentLocator.elementHandle();\n    const __r3Current = __r3CurrentNode ? await __r3CurrentNode.evaluate((element) => ({\n        connected: element.isConnected,\n        active: document.activeElement === element,\n        visible: Boolean(element.getClientRects().length),\n        tag: element.tagName,\n        suggestion: element.closest('[data-qualification-suggestion]')?.getAttribute('data-qualification-suggestion') || null,\n    })) : null;\n    const __r3SameNode = __r3CurrentNode ? await page.evaluate(({ oldNode, currentNode }) => oldNode === currentNode, { oldNode: __r3OldNode, currentNode: __r3CurrentNode }) : false;\n    console.log('R3_QUALIFICATION_OPTIONAL_CONFIG_INTERCEPTED=' + String(__r3OptionalConfigIntercepted));\n    console.log('R3_QUALIFICATION_NODE_AFTER_OLD=' + JSON.stringify(__r3AfterOld));\n    console.log('R3_QUALIFICATION_NODE_AFTER_CURRENT=' + JSON.stringify(__r3Current));\n    console.log('R3_QUALIFICATION_SAME_NODE=' + String(__r3SameNode));\n    console.log('OLD_NODE_REPLACED_BY_VALID_RERENDER=' + String(!__r3AfterOld.connected && Boolean(__r3Current?.connected && __r3Current?.visible) && !__r3SameNode));\n    return;\n    const controls = [`;

if (!text.includes(needle)) throw new Error('R3_DIAG_NEEDLE_NOT_FOUND');
text = text.replace(needle, replacement);
writeFileSync(target, text);
console.log(`R3_DIAG_WRITTEN=${target}`);
